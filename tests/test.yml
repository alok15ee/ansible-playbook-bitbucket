---
# test file for bitbucket

- hosts: localhost
  remote_user: root
  roles:
    - bitbucket
    - mysql_connector_java
    - mysql
    - mysql_user
    - mysql_db
    - postgresql
    - postgresql_user
    - postgresql_db
    - apache2
    - apache2_proxy
